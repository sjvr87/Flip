# Cursor Auto-Permissions for Flip Dev

## Overview
This project uses auto-allow permissions for both Copilot and Cursor agents. All development work is pre-authorized to minimize approval delays.

## Setup (One-time)

### 1. Cursor UI Settings
- Open: **Settings → Cursor Settings → Agents → Approvals & Execution**
- Set: **Run Mode → Run Everything**
- Reload: `Ctrl+Shift+P` → **Developer: Reload Window**

This disables approval prompts for all authorized commands.

### 2. Permission Files (Already in Repo)
- `.cursor/permissions.json` — Flip-specific allowlists + auto-approve rules
- Global user config: `C:\Users\tomas\.cursor\permissions.json` (if needed for cross-project)

## What's Auto-Allowed

✅ **Git & Version Control**
- `git pull`, `git push`, `git checkout`, `git merge`

✅ **Package Management**
- `npm install`, `npm run` (all scripts)

✅ **Android Development**
- `adb devices`, `adb reverse`, `adb shell`, `adb push`, `adb pull`
- `gradle build`, `./gradlew`
- `expo prebuild`, `expo run:android`, `expo start`

✅ **Device Testing**
- `flip-*.bat` scripts (dev-connect, reset, reload, etc.)
- Metro bundler operations
- App launching and debugging

✅ **Workspace Operations**
- File writes within project directory
- Network calls (GitHub API, package registries, etc.)
- Full sandbox disable for builds

## What's Still Blocked

❌ **Secrets & Security**
- Cannot commit `.env` files or credentials
- Cannot force push to `main` or `master`

❌ **Destructive Operations**
- Some delete/reset operations may still prompt for confirmation

## Agent Coordination

Both Copilot and Cursor operate under these rules:

1. **No approval chains** — execute immediately when authorized
2. **No context switching** — stay on current task/blocker
3. **Tag & sync only** — communicate on completion or hard blocker
4. **Auto-merge PRs** — other agent approves if code is sound

See `.github/AGENT_COORDINATION.md` for full workflow.

## Troubleshooting

**Approval prompt still appearing?**
- Verify Run Mode is set to "Run Everything" (not Smart Mode)
- Check that `permissions.json` is in `.cursor/` (repo-level, not workspace root)
- Reload Cursor: `Ctrl+Shift+P` → Developer: Reload Window

**Command not in allowlist?**
- Edit `.cursor/permissions.json` → `terminalAllowlist` array
- Add the command prefix (e.g., `my-script`) and reload

---

**Last updated:** 2026-06-23
**Applies to:** All Copilot + Cursor sessions on this repo